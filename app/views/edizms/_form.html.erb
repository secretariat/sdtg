<%= form_for(@edizm) do |f| %>
  <% if @edizm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edizm.errors.count, "error") %> prohibited this edizm from being saved:</h2>

      <ul>
      <% @edizm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-control text span4">
    <%= f.text_field :name %>
  </div>
  <br/>
  <%= f.select :lang, { "RU" => "ru", "EN" => "en"} %>
  <h4><%= f.check_box(:visible) %>Опубликовать</h4>
  <br/>
  <br/>
  <div class="actions">
    <%= link_to 'Назад', edizms_path, :class=>"button big bg-color-blueDark", :style => "color: white;" %>
    <%= f.submit :'Сохранить', class: "default" %>
  </div>
<% end %>
